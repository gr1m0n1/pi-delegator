# Pi Delegator Activity

Live activity view for Pi subagents. Open the Pi Delegator icon in the VS Code activity bar after installing the extension.

The extension reads `.pi-delegator/logs/pi-agents.jsonl` in the first workspace folder. Set `piDelegator.activity.logPath` when the Pi runtime lives elsewhere.